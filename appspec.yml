version: 0.0
os: linux
files:
  - source: /
    destination: /var/www/html/
hooks:
  BeforeInstall:
    - location: scripts/clean_files.sh
      timeout: 300
      runas: root
  ApplicationStart:
    - location: scripts/npm_i_start.sh
      timeout: 300
      runas: root
